import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        text: {
            primary: "#081F4A",
            secondary: "#5B6773",
        },
    },
    typography: {
        fontFamily: "var(--body-font)",
    },
});